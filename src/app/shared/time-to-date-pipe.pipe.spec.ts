import { TimeToDatePipePipe } from './time-to-date-pipe.pipe';

describe('TimeToDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeToDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
