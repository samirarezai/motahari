import { LocalSearchPipe } from './local-search.pipe';

describe('LocalSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new LocalSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
