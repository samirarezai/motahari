import { LectureDirectiveDirective } from './lecture-directive.directive';

describe('LectureDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new LectureDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
